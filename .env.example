# P2P Growth Engine Environment Variables

# Webhook Secret for Ingestion (Must match the header 'x-p2p-secret' sent by the webhook source)
P2P_INGEST_SECRET=change_me_to_a_secure_random_string

# Optional: Database Connection String (e.g., Supabase / Postgres)
DATABASE_URL=

# Optional: AI Model API Keys
OPENAI_API_KEY=
GEMINI_API_KEY=
